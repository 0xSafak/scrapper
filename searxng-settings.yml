# Minimal overlay so the discovery tool can use format=json.
# Mount this as /etc/searxng/settings.yml when running SearXNG Docker.
# See README: "Using SearXNG (local, no quota)".
use_default_settings: true
server:
  secret_key: "scrapper-local-searxng-2024-a1b2c3d4e5f6"
search:
  formats:
    - html
    - json
