# ── Search Provider ────────────────────────────────────────────────────
# Set the provider in config.json (discovery.provider): serpapi | searxng | google

# SerpAPI — reliable cloud search API (free tier available)
# Get your key at https://serpapi.com/
SERPAPI_API_KEY=your_serpapi_key_here

# SearXNG — self-hosted, no API key needed. Run with Docker:
#   docker run -d -p 8081:8080 -v "$(pwd)/searxng-settings.yml:/etc/searxng/settings.yml" --name searxng searxng/searxng
# SEARXNG_URL=http://127.0.0.1:8081

# Google Custom Search — 100 free queries/day
# Set up at https://programmablesearchengine.google.com/
# GOOGLE_CSE_API_KEY=your_google_api_key
# GOOGLE_CSE_CX=your_search_engine_id

# ── AI Extraction (optional) ──────────────────────────────────────────
# OpenRouter API key for AI-assisted email extraction
# If not set, the tool uses regex-only extraction (still works well)
# Get a free key at https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_key_here

# ── Optional ──────────────────────────────────────────────────────────
# Custom User-Agent for HTTP requests (default is a Chrome-like UA)
# USER_AGENT=Mozilla/5.0 (compatible; Scrapper/2.0)
